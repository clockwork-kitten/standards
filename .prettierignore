# Markdown and the generated doc index are owned by canon (orchestrated by
# conform). Keep Prettier out so the two formatters never fight.
*.md
**/*.md
llms.txt

# Tool/CI config owned elsewhere: workflow YAML is CI infrastructure — not
# application code Prettier should churn.
.github/

# Generated / vendored.
bun.lock
node_modules
coverage
dist
build

# Astro fixtures are formatted by `prettier-plugin-astro` in consumer repos (see
# `configs/prettier.astro.json`), not by this repo's base Prettier config.
**/*.astro
