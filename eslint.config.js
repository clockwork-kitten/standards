/**
 * Dogfood: the standards repo lints its own engine with the studio base it ships.
 * Consumers do the same — import the base and extend as needed.
 */

export { default } from "@clockwork-kitten/conform/configs/eslint.base";
